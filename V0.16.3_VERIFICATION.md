# KV Archive v0.16.3 Verification

State: `LOCALLY_VERIFIED`

## Executed

- `node --test tests/export-artifact.test.mjs` — PASS, 6/6.
- `npm run typecheck` — PASS.
- `npm test` — PASS, 250/250 after clean Core, extension, PWA and Agent Bridge builds.

## Important assertions

- a real conversation title becomes the HTML/Markdown/ZIP outer filename;
- a title such as `VectCutAPI 开源项目分析 - ChatGPT` becomes `VectCutAPI 开源项目分析.html`;
- generic provider titles fall back to the first user message;
- Windows-invalid characters and reserved names remain portable;
- existing readable HTML and technical ZIP contents are unchanged.

## Browser boundary

The actual filename shown by the user’s configured Chrome download environment still requires installed-extension acceptance. Chrome may append `(1)`, `(2)` and similar suffixes when a file with the same name already exists.
