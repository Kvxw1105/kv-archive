# v0.16.1 Changeset

- Added `conversation-catalog-store.js` and IndexedDB `conversation-catalogs`.
- Basket restores the latest saved selection and cached ChatGPT catalog.
- Basket selection/title/filter changes persist automatically.
- Catalog refresh writes cache; refresh failure preserves prior cached data.
- Added `workspace-navigation.js` to reuse one KV Archive workspace tab.
- Backup-to-library transition now stays in the current workspace tab.
- Renamed “Memory Sync” UI to “Project Context” and added plain-language Project guidance.
- Added automatic/saved Project selection behavior.
- Added regression tests for persistence, workspace reuse and terminology.
