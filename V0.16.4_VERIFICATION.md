# KV Archive v0.16.4 Verification

State: `LOCALLY_VERIFIED`

## Automated verification

- Clean build: core, extension, Notes PWA, and Agent Bridge passed.
- Complete suite: 255/255 passed.
- Ten-minute acceptance tests cover:
  - exact ten-minute one-time alarm creation;
  - refusal without a logical snapshot baseline;
  - automatic incremental execution with added and updated conversations;
  - successful alarm and fetch with no content changes;
  - startup alarm reconstruction and waiting-test cancellation.
- The test path was verified with recurring scheduling disabled and idle state active, proving that acceptance uses its explicit bypass rather than changing production settings.

## Performance gates

- 1,200-conversation low-memory archive: 29 volumes; maximum concurrent artifact reads 1.
- 3,000-conversation snapshot gate passed.
- 3,000-conversation Obsidian graph: 0 broken links and 0 invalid Canvas references.
- 3,000-object Portable Capture dry-run passed.
- 3,000-object Verified Handoff preflight passed.
- 10,000-note PWA list gate passed in 22 ms with first render bounded to 80 rows.

## Browser evidence boundary

A Chromium 144 process was available, but the container could not expose a loaded extension service-worker target through its headless/Xvfb setup. The environment also has no authenticated ChatGPT account. Therefore no claim is made that a real logged-in account completed the ten-minute run.
