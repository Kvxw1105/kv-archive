# KV Archive v0.13.3 Verification

Status: `LOCALLY_VERIFIED`

## Automated suite

```text
npm test
168 tests
168 passed
0 failed
```

The suite covers all previous backup, Basket, provider, Agent Bridge, Continuity Benchmark, Project State, Memory, scheduler and Obsidian behavior, plus the v0.13.3 routing and DOM-hydration regressions.

## Targeted verification

Verified locally:

- nested ChatGPT Project/GPT paths remain structured conversation URLs;
- nested paths yield the correct conversation ID;
- four overlapping virtualized message windows merge into 50 ordered messages;
- repeated identical content from different stable turn IDs is preserved;
- uncertain structured capture is compared against hydrated DOM count;
- compact-mode semantic filtering remains intact;
- partial HTML renders A-to-D pipeline diagnostics.

## Static and build checks

```text
npm run typecheck
npm run build
node --check apps/extension/dist/background.js
node --check apps/extension/dist/generic-dom-adapter.js
node --check apps/extension/dist/popup.js
```

All passed.

## Performance regression

### Historical archive

- conversations: 1,200;
- approximate source text: 19,660,800 bytes;
- ZIP volumes: 29;
- maximum concurrent artifact reads: 1;
- peak RSS delta: 95,965,184 bytes.

### Snapshot

- conversations: 3,000;
- unique assets: 120;
- changed conversations: 200;
- snapshot and garbage-collection smoke passed.

### Obsidian

- conversations: 3,000;
- nodes: 3,002;
- edges: 6,001;
- volumes: 2;
- status: COMPLETE;
- broken links: 0;
- invalid Canvas references: 0.

## Version consistency

- root package: 0.13.3;
- extension source manifest: 0.13.3;
- built extension manifest: 0.13.3;
- Agent Bridge package: 0.13.3.

## Not verified here

- the exact original private conversation in the user's logged-in browser;
- the current real ChatGPT Project/GPT DOM selectors;
- lazy-load timing under very slow networks;
- branch semantics when only DOM hydration is available;
- Chrome profile upgrade and existing IndexedDB preservation;
- commit, push, PR, CI or public release.
