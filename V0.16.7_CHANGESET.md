# KV Archive v0.16.7 Changeset

## Added

- `apps/extension/src/visibility.css`
  - semantic compatibility aliases;
  - explicit control-state color pairs;
  - per-page type and contrast corrections;
  - 12 px interactive-copy floor;
  - native `[hidden]` protection.
- `apps/pwa/src/visibility.css`
  - readable note/control text;
  - explicit disabled states;
  - separate dark/light button carrier colors.
- `tests/ui-visibility.test.mjs`
  - page load-order coverage;
  - token contrast checks;
  - unresolved CSS variable detection;
  - hidden-state and typography-floor checks;
  - PWA white-on-color control contrast checks.

## Updated

- Every extension HTML surface loads `visibility.css` after `theme.css`.
- Notes PWA loads and offline-caches `visibility.css`.
- Current package, extension, PWA and Agent Bridge versions are `0.16.7`.

## Protected

- Raw Evidence and all database schemas.
- Project State and Memory Gate semantics.
- Portable Capture, Verified Handoff and archive formats.
- Existing `system`, `light`, `dark` appearance modes.
