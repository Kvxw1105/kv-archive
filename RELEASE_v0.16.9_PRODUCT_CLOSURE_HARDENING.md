# KV Archive v0.16.9 — Product Closure & Safety Hardening

状态：`LOCALLY_VERIFIED`

本版不是新增一批展示性功能，而是审计并修复“入口存在但实际闭环不完整、名称与真实影响不一致、数据量变大后不可用”的产品缺口。

## 关键安全修复

### “清空会话索引”不再误删治理数据

旧实现的清理范围过大，可能连带删除 Project State、项目上下文、Memory Gate、知识图谱配置及其他治理记录。更危险的是，如果只保留治理记录却删除其 Evidence，已有证据链接会失去可追溯性。

v0.16.9 将该操作收紧为：

- 删除可搜索会话目录；
- 删除消息搜索索引；
- 删除导入记录；
- 保留 Raw Evidence；
- 保留 Project State、提案、版本与审计；
- 保留批准记忆、Memory Gate 回执、记录中心、恢复回执和 Obsidian 配置。

按钮和确认文案同步改为“清空会话索引”，不再把局部清理伪装成“清空资料库”。

## 产品闭环修复

- 搜索取消 5,000 条候选消息静默截断，并以受控批次读取全部候选。
- 搜索结果、超长对话、提案、状态版本、记忆版本、记录版本、操作日志、关系和恢复回执均提供分批加载。
- 快速连续搜索或 Project 切换时，旧请求不能覆盖新页面。
- 记录中心可直接新建 Project；仅由笔记建立的 Project 也能进入项目状态、项目上下文和 Obsidian。
- 项目上下文真正纳入本地笔记及稳定 Content Evidence URI，不再只认聊天记录。
- 同名 Project 可复用；不同名称规范化为同一 ID 时明确阻止，禁止静默合并数据。
- 保存笔记后保留当前 Project，方便连续记录；手动“清空编辑区”才完全重置。
- 草稿保存失败必须就地报错，不再假装已自动保存。
- 归档、恢复、撤销和关系操作按真实原状态执行。
- 关系不足两个有效对象时，建立关系入口不可用且说明原因。
- Agent 接力包明确区分屏幕搜索条件与导出范围，并只读取目标 Project 所需消息和 Evidence，避免全库读入。
- Notes PWA 的“操作与关系”显示真实记录而非只有数量，并提供安装说明与历史分页。

## 兼容性

- IndexedDB 版本保持 13，无数据库迁移。
- Raw Evidence、Portable Capture、Project State、Memory Gate 和 Agent Bundle 格式不变。
- 旧数据不需要重建。
