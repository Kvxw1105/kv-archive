# KV Archive v0.12.1｜HTML 内容控制与技术链路折叠

## 本次修复

本版本针对单个对话的 HTML、Markdown 与当前对话 ZIP 导出，增加明确的内容范围选择。

### 默认：精简对话

默认只导出：

- 用户问题；
- AI 正式回答；
- 正式回答中可见的代码、图片、文件、引用与链接。

默认不导出：

- 工具调用；
- 工具结果；
- 推理摘要；
- 系统消息；
- 未识别的内部事件；
- 原始技术证据 JSON。

### 可选：仅 AI 正式回答

只保留 AI 的正式回答，适合交给本地 Agent 阅读、整理或二次加工，进一步降低无关上下文。

### 可选：完整技术记录

用户主动选择后，才包含工具调用、工具结果、可见推理摘要和内部事件。HTML 中所有技术内容使用折叠块，默认关闭；AI 正式回答仍默认展开。

当选择当前对话 ZIP 时，raw.json、canonical.json 与 technical-evidence 文件也只在“完整技术记录”档进入压缩包。

## 兼容性

- 不修改 IndexedDB schema；
- 不修改完整历史备份引擎；
- 不影响 Agent Bundle 已有的内部事件过滤；
- 不影响 Obsidian 导出。

## 验证

- `npm test`：142/142 通过；
- TypeScript 构建通过；
- 扩展构建通过；
- 变更 JS 文件通过 `node --check`；
- 真实 Chrome 页面导出仍需用户验收。
