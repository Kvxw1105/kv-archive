# KV Archive v0.16.6 Verification

State: `LOCALLY_VERIFIED`

## Build and static verification

- `npm run typecheck` — passed.
- `npm test` — 263 tests passed, 0 failed.
- Extension, PWA, Core and Agent Bridge builds — passed.
- PostCSS parsing of shared and theme CSS — passed.
- Theme assets confirmed in extension and PWA build outputs.

## Theme contract tests

- bootstrap runs before styles on all extension surfaces;
- modes are limited to `system`, `light`, `dark`;
- system mode resolves from `prefers-color-scheme`;
- explicit modes override system preference;
- local persistence and cross-page storage updates are present;
- OS appearance changes are observed in system mode;
- controls expose `aria-pressed` and keyboard navigation;
- PWA caches the full theme runtime offline;
- reduced-motion handling is retained.

## Contrast checks

Representative token pairs:

- dark primary text on panel: 16.23:1;
- dark muted text on panel: 8.00:1;
- light primary text on panel: 15.43:1;
- light muted text on panel: 5.27:1;
- light primary button text/background: 9.80:1;
- dark primary button text/background: 15.50:1.

## Performance gates

- 1,200-conversation low-memory archive: passed; 29 volumes; maximum concurrent artifact reads 1.
- 3,000-conversation snapshot: passed.
- 3,000-conversation Obsidian graph: 0 broken links, 0 invalid Canvas references.
- 3,000-object Portable Capture recovery: passed; 9,000 planned writes.
- 3,000-object Verified Handoff: preflight PASS; one scoped Project.
- 10,000-row PWA list: passed; first render bounded to 80 rows.

## Unverified external paths

- Real Chrome extension rendering under actual OS light/dark changes.
- Mixed-monitor color profile and high-contrast mode.
- Popup height and side-rail theme control at user-specific zoom levels.
- Real Android Chrome and iPhone Safari PWA appearance switching.
