# v0.16.11 Changeset

## Runtime

- `apps/extension/src/backup.js` — register scheduler acceptance countdown/progress elements; add defensive renderer guard; show per-cycle reused bytes/nodes.
- `apps/extension/src/backup.html` — rename the dedup metric to `本轮复用节省`.
- `apps/extension/src/conversation-chunks.js` — new content-addressed conversation node, mapping-reference chunk and manifest planning/reconstruction layer.
- `apps/extension/src/history-store.js` — bounded bulk content-object existence/write/read helpers; store chunked conversations through existing `putArtifact()` and reconstruct through existing `getArtifact()`.
- `apps/extension/src/history-engine.js` — accumulate/reset per-cycle inserted/reused object and byte counters.
- `apps/extension/src/content-snapshots.js` — persist reuse metrics and expand content-object references transitively before GC.

## Tests/performance

- scheduler DOM-registration regression;
- exact raw graph reconstruction and continued-conversation reuse tests;
- history-store wiring/schema-boundary regression;
- two-level manifest → mapping chunk → node GC regression;
- 10,000-node continued-conversation performance smoke.

## Version

Extension, Notes PWA, Agent Bridge and root package metadata: `0.16.11`. Extension DB remains v13; Notes PWA DB remains v1.
