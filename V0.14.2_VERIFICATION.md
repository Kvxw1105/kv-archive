# KV Archive v0.14.2 Verification

Status: `LOCALLY_VERIFIED`

## Automated verification

- Full suite: 209/209 passed.
- TypeScript clean build: passed.
- Browser extension build: passed.
- Agent Bridge build: passed.
- Modified JavaScript syntax checks: passed.

## New coverage

- provider-neutral content normalization and stable evidence URI;
- immutable revisions and stale-revision conflict rejection;
- typed relations, cross-Project relation rejection and operation-log parity;
- review-required Memory/Decision/Task promotion;
- content record export with all versions, relations, operations and promotions;
- IndexedDB schema separation between Raw Evidence and editable captures;
- Bundle v1/v2 backward compatibility and Bundle v3 content files;
- Agent content search/read and MCP tools;
- mixed conversation/content Context Packs;
- content graph nodes and typed edges;
- notes-only Project Obsidian export;
- missing content evidence detection;
- Capture Center presence in the shared application shell.

## Performance regression

### Low-memory archive

- conversations: 1,200;
- source text: approximately 19.66 MB;
- ZIP volumes: 29;
- maximum concurrent artifact reads: 1;
- peak heap delta: 15,731,048 bytes.

### Snapshot smoke

- conversations: 3,000;
- changed conversations: 200;
- unique assets: 120;
- deduplicated bytes saved: 3,932,160;
- objects reclaimed: 200.

### Obsidian smoke

- conversations: 3,000;
- nodes: 3,002;
- edges: 6,001;
- volumes: 2;
- broken links: 0;
- invalid Canvas references: 0;
- status: COMPLETE.

## Package/version checks required before delivery

- root package, source Manifest, built Manifest and Agent Bridge must all report 0.14.2;
- ZIP CRC and path safety must pass;
- source package must exclude dependencies, temporary performance outputs and credentials;
- release hashes must be written after final packaging.

## Not verified externally

- real Chrome Capture Center interaction;
- IndexedDB 10 → 11 upgrade against the user's existing profile;
- real concurrent-edit conflict behavior across multiple extension pages;
- real notes-only Obsidian Vault opening;
- real Agent Bundle v3 use in an external receiving Agent;
- Git commit, push, PR, remote CI or public release.
