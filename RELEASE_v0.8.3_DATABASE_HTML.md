# ContextVault v0.8.3｜数据库兼容与 ChatGPT 风格 HTML

## 修复

- 将备份、资料库和状态中心统一到同一个 IndexedDB Schema 入口。
- 数据库版本从 4 升级到 5，修复备份页请求版本 3、已有数据库为版本 4 时出现的 `VersionError`。
- 升级被旧页面阻塞时给出可执行的中文提示，不再只显示浏览器原始错误。

## HTML 阅读体验

- 导出页面改为接近 ChatGPT 原版的消息流布局。
- 用户消息右侧气泡；Assistant 消息采用正文流和头像布局。
- 增加固定滚动目录，目录以用户问题摘要为节点并跟随当前阅读位置。
- 增加全文搜索、精确高亮、上一处、下一处和快捷键。
- 增加消息复制、代码复制、明暗主题和移动端目录抽屉。
- 增加有序列表、表格、引用、分隔线和代码语言栏渲染。
- Canonical、Raw、Agent 索引与可读 HTML 继续分层，视觉升级不会删减证据数据。

## 版本

- Extension: 0.8.3
- IndexedDB: 5
