# KV Archive v0.16.4 Changeset

## Added

- Ten-minute one-time scheduled incremental acceptance panel in Backup Center.
- Durable test runtime with baseline snapshot, due time, alarm firing, completion result, and sanitized failure state.
- Independent `context-vault-scheduled-backup-test` Chrome Alarm.
- Automatic comparison of baseline and latest conversations, assets, and Projects.
- Explicit `passed_with_changes` and `passed_no_changes` outcomes.
- Startup reconciliation for waiting tests and time-sliced test continuations.
- Five-second status refresh while acceptance is active.

## Preserved

- Existing 1/3/7/30-day recurring schedule settings.
- Browser-local incremental archive and content-addressed deduplication.
- Manual ZIP export as a separate user action.
- Raw Evidence, Project State, Memory Gate, Portable Capture, Agent Bundle, PWA, and Verified Handoff contracts.
- IndexedDB schema version; no migration is required.
