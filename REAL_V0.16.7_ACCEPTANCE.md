# Real Chrome Acceptance — KV Archive v0.16.7

Use a real installed extension in both Light and Dark modes.

## Popup

1. Open the popup at 100% Chrome zoom.
2. Confirm title, guidance, destination cards, primary/secondary buttons, quick entries and status copy are readable.
3. Confirm unavailable recovery actions remain completely hidden; no blank buttons appear.
4. Repeat at 80%, 125% and 150% zoom.

## Workspace pages

Open every page in both themes:

- Backup Center;
- Conversation Basket;
- Local Library;
- Capture Center;
- Project State;
- Project Context;
- Obsidian Graph.

For each page verify:

- normal, hover, focused and disabled buttons remain readable;
- labels and secondary copy do not disappear into cards;
- selected navigation remains identifiable;
- code/editor surfaces use high-contrast text;
- no larger text clips, overlaps or creates horizontal overflow.

## System mode

1. Select Follow system.
2. Change the operating-system appearance from light to dark and back.
3. Confirm open KV Archive surfaces update without reload and remain readable.

## Data-populated checks

Run at least one real workflow so populated states appear:

- Backup Health and scheduler summary;
- Conversation Basket selection and progress;
- Project Context / Memory Gate result table;
- Obsidian preview and quality gate;
- one success, one failure and one disabled state.

## Notes PWA

Check the floating `＋` button, primary actions, disabled actions, bottom navigation, note cards and filters in light/dark modes on an actual phone when available.

Record Chrome version, OS, zoom, theme, page/control and screenshot for any remaining issue.
