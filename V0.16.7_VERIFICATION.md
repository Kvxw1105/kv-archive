# KV Archive v0.16.7 Verification

State: `LOCALLY_VERIFIED`
Date: 2026-08-06

## Build and regression

- `npm run typecheck`: PASS.
- `npm run build`: PASS for Core, Extension, Notes PWA and Agent Bridge.
- `npm test`: **269 / 269 PASS**, 0 failed.

## Chromium visibility audit

The audit rendered source HTML and all linked CSS inside system Chromium using Playwright `page.set_content`.

Coverage:

- 8 extension surfaces × light/dark = 16 renders;
- popup viewport 430 px wide;
- workspace viewport 1440 px wide;
- Notes PWA viewport 390 × 844 in light/dark;
- visible text size and computed foreground/background contrast;
- normal, hover and focus states for buttons, links, summaries and form controls.

Results:

- extension static text/contrast findings: 0;
- extension control-state findings: 0;
- Notes PWA findings: 0.

Acceptance thresholds:

- visible functional text: at least 11 px;
- interactive microcopy: at least 12 px;
- ordinary/control text contrast: at least 4.5:1.

## Performance gates

- Archive: 1,200 conversations, 29 ZIP volumes, max concurrent artifact reads 1.
- Snapshot: 3,000 conversations, 200 changed conversations.
- Obsidian: 3,000 conversations, 3,002 nodes, 6,001 edges, 0 broken links, 0 invalid Canvas references.
- Portable Capture: 3,000 objects, 9,000 planned writes, package validation and dry-run PASS.
- Verified Handoff: 3,000 objects, one scoped Project, preflight PASS.
- Notes PWA: 10,000 rows, first render bounded to 80, search/filter performance PASS.

## Package verification required before delivery

The final delivery process must still verify ZIP CRC, path safety, root Manifest/index placement, current versions, nested packages, temporary-file exclusion and SHA-256 checksums.

## Not verified here

- owner's installed Chrome profile and extension APIs;
- 80%–150% real browser zoom;
- operating-system high-contrast modes;
- real data-populated tables, long translated strings and extension runtime state changes;
- Android Chrome and iPhone Safari PWA rendering;
- Git commit, push, PR, CI or public release.
