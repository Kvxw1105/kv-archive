# KV Archive v0.12.2 真实环境验收

Status: REAL_ENVIRONMENT_ACCEPTANCE_PENDING

## 0. 安装与数据保护

1. 先保留一份已有电脑备份；
2. 不要在 Chrome 中删除旧扩展；
3. 将 v0.12.2 解压到固定目录；
4. 在 `chrome://extensions/` 中对原 KV Archive 点击“重新加载”，或让原扩展目录指向新版文件；
5. 确认版本为 `0.12.2`；
6. 打开资料库，确认旧对话、索引和项目状态仍存在。

通过条件：原扩展身份和 IndexedDB 数据保留，没有出现空库或数据库迁移错误。

## A. 新用户弹窗路径

1. 打开普通网页后点击 KV Archive；
2. 再打开一条具体 ChatGPT `/c/` 对话并点击 KV Archive。

期望：

- 普通网页提示先打开具体对话，并提供“打开 ChatGPT”；
- 对话页显示一个明确推荐下一步；
- 首屏常用入口只有备份中心和本地资料库；
- 长期项目与高级工具默认收起；
- 100%、125%、150% 缩放下没有遮挡或横向溢出。

## B. 当前对话默认保存到资料库

1. 保持“收进本地资料库”选中；
2. 不勾选“同时下载文件”；
3. 点击“保存当前对话”；
4. 保存后点击“立即查看”；
5. 在资料库搜索该对话中的独特关键词。

期望：

- 不产生下载文件；
- 对话进入本地资料库；
- 搜索能够找到；
- 成功提示不会被后续状态刷新覆盖；
- 重复保存不会产生失控的重复数据。

## C. 同时下载文件

1. 勾选“同时下载文件”；
2. 分别测试 HTML、Markdown 和 ZIP 中至少一种；
3. 保存完成后点击“显示下载文件”。

期望：

- 本地资料库和下载文件同时成功；
- 下载文件内容符合选择的精简、仅 AI 或技术模式；
- “显示下载文件”定位到正确下载记录或文件位置。

## D. 备份健康卡

打开备份中心，分别在以下状态检查：

- 从未备份；
- 采集进行中或暂停；
- 对话采集完成但未生成 ZIP；
- ZIP 分卷全部完成；
- 有失败对话或缺失附件。

期望：

- 首屏明确区分浏览器本地与电脑备份文件；
- 显示浏览器存储估算；
- 告诉用户下一步，而不是只展示技术数字；
- 详细统计默认收起，展开后旧指标仍完整。

## E. 开始前预检

1. 点击“开始全量备份”；
2. 不立即确认；
3. 核对工作空间、预计对话、Project、附件策略和容量；
4. 取消一次；
5. 再次预检并确认。

期望：

- 取消预检不会启动采集；
- 确认前不修改 ChatGPT 内容；
- 有旧检查点时明确说明会继续而不是从头开始；
- 确认后才进入原有备份引擎；
- 请求持久化存储失败不会阻断备份。

## F. 错误恢复

至少验证：

1. 没有 ChatGPT 标签页；
2. 登录失效；
3. 选择错误工作空间或任务空间不一致；
4. 下载被浏览器中断。

期望：每种错误都有直接动作，如打开 ChatGPT、重新检测、检查工作空间、查看下载记录或重试；已完成的数据和检查点不丢失。

## G. Obsidian 质量结论

1. 选择一个可导出的 Project 并分析；
2. 再使用一个包含缺失附件或人为测试结构问题的样本。

期望：

- 先看到“可以安全导出”“可以导出但有缺失”或“暂不建议导出”；
- 技术质量指标默认收起；
- 展开后数字与原质量门一致；
- 导出和复制 Agent 交接指令仍可使用。

## 未通过时记录

```text
页面：
账号/工作空间：
Chrome 与系统版本：
操作步骤：
实际结果：
预期结果：
是否稳定复现：
Console 报错：
截图：
```
