# ContextVault v0.3.0｜分批交付 1：普通历史对话备份

## 本批次目标

把“当前会话导出器”推进为可持续备份工具，完成普通历史对话的批量索引、逐条保存、中断恢复、失败重试和单 ZIP 交付。

## 已实现

- 保留 v0.2.0 当前会话单文件导出。
- 新增独立“历史备份中心”页面。
- 在已登录 ChatGPT 页面上下文发起同源请求，无需复制 Bearer Token。
- 普通会话列表分页、ID 去重和安全终止条件。
- 对话逐条获取，单条失败不拖垮全部任务。
- 429、408、409、5xx 和网络错误的有限指数退避。
- 登录失效时暂停并保留进度。
- IndexedDB 保存任务、元数据和已完成原始会话。
- 关闭备份页后可继续，已完成对话不会重复获取。
- “完成但有失败项”可只重试失败条目。
- 最终只下载一个 ZIP。
- ZIP 内含 `index.html` 总目录，以及每条对话的 HTML、Markdown、raw.json、canonical.json 和完整性报告。
- ZIP 入口数量、单文件大小、总偏移和预计内存的安全限制。

## 本批次明确不包含

- Projects；
- 归档会话；
- 附件和图片二进制；
- Deep Research 过程流；
- SQLite 本地资产仓；
- MCP / Agent 接力。

这些内容进入后续独立批次，不在界面上冒充已支持。

## 用户路径

1. 安装扩展并保持一个已登录 ChatGPT 标签页打开。
2. 点击扩展。
3. 选择“备份普通历史对话”。
4. 在独立进度页点击“开始备份”。
5. 可暂停、关闭页面、稍后继续。
6. 完成后浏览器只下载一个 ZIP。
7. 解压 ZIP，双击 `index.html` 浏览全部已保存对话。

## 状态

- 代码实现：完成。
- 类型检查：完成。
- 自动化测试：22/22 通过。
- ZIP 系统解压校验：通过。
- 当前会话真实浏览器路径：用户已验证 v0.2.0 基本可运行。
- 普通历史真实账号批量路径：需要用户在本机执行首次验收。
