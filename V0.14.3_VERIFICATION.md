# KV Archive v0.14.3 Verification

Status: `LOCALLY_VERIFIED`

## Commands

```bash
npm test
npm run test:capture-recovery-performance
npm run test:performance
npm run test:snapshot-performance
npm run test:obsidian-performance
```

## Results

- automated tests: 219 passed, 0 failed;
- TypeScript/core build: passed;
- extension build: passed;
- Agent Bridge build: passed;
- JavaScript syntax checks: passed.

## Portable recovery performance

- content objects: 3,000;
- immutable versions: 3,000;
- operations: 3,000;
- planned writes into an empty Vault: 9,000;
- ZIP size: 8,772,628 bytes;
- record generation: 472 ms;
- package build, hash validation and dry-run planning: 2,568 ms;
- total: 3,040 ms.

These timings describe the local test environment and are not a browser SLA.

## Existing performance regression

- history conversations: 1,200;
- volume count: 29;
- max concurrent artifact reads: 1;
- snapshot conversations: 3,000;
- Obsidian conversations: 3,000;
- graph nodes: 3,002;
- graph edges: 6,001;
- broken links: 0;
- invalid Canvas references: 0.

## Covered recovery cases

- deterministic package identity and ZIP round-trip;
- schema, count, object hash and payload hash validation;
- dry-run zero-write behavior;
- new-object recovery;
- identical re-import no-op receipt;
- Project conflict;
- same-revision hash conflict;
- divergent history conflict;
- exact-lineage fast-forward;
- boundary-safe rollback;
- later edits blocking rollback;
- dependent relation and promotion blocking rollback;
- repeated rollback blocked;
- unbound scope isolation;
- scope/content mismatch rejection;
- Capture Center recovery controls;
- IndexedDB schema 12 creation.

## Not verified here

- real Chrome IndexedDB 11 → 12 in-place upgrade;
- real download/upload permission behavior;
- large browser ZIP memory behavior on the user's machine;
- cross-page write races during an active import;
- actual rollback after browser restart;
- Git commit, push, PR, CI or public release.
