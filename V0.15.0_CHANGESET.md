# v0.15.0 Changeset

## Added

- `apps/agent/src/verified-handoff.js`
- `apps/agent/src/bundle-builder.js`
- `apps/agent/src/zip-writer.js`
- `tests/verified-handoff.test.mjs`
- `scripts/verified-handoff-performance-smoke.mjs`
- Agent CLI commands `handoff-create`, `handoff-receive`, `handoff-verify`
- POSIX and Windows wrapper scripts in the Agent Bridge package
- sender delivery receipt, receiver preflight receipt and completion receipt
- Project-scoped Agent Bundle generation
- private verification-kit separation

## Changed

- Agent Bundle parser can load from bytes as well as a file path.
- Agent Bridge generated documentation includes the Verified Handoff workflow.
- Root, extension and Agent Bridge versions are unified at `0.15.0`.

## Security and integrity

- blocks undeclared/duplicate/unsafe entries;
- blocks private answer keys in receiver packages even when outer hashes are recomputed;
- cross-checks contract, sender receipt, embedded Bundle, approved State, Context Pack, Gate and Benchmark identities;
- refuses overwrite of receipts and generated outputs;
- never mutates approved Project State.
