# v0.16.10 Changeset

## Engineering handoff truth

- Replaced the stale root `AGENTS.md` that still described the repository as starting in `PLANNED` state.
- Updated `.agent-harness/CURRENT_STATE.md` from v0.16.2 to the actual v0.16.10 baseline.
- Added TASK-028 with explicit protected contracts and external acceptance gaps.

## Scoped storage reads

- Project-scoped Agent export now uses the existing `vault-conversations.projectId` and `capture-items.projectId` indexes and direct Project State lookup before loading selected messages/Evidence.
- Extension Capture export reads `capture-versions` by the selected content-object IDs through the existing `objectId` index.
- Notes PWA uses the same bounded content-version strategy for Project/export/recovery paths.
- Full-vault export behavior remains available when no Project scope is supplied.

## PWA isolation

- Service Worker activation now deletes only stale caches whose names begin with `kv-archive-notes-`.
- Unrelated Cache Storage entries on the same origin are left untouched.

## Version consistency

- Extension, Notes PWA, Agent Bridge and root package versions are aligned on `0.16.10`.
- Agent Bundle and Portable Capture fallback `sourceAppVersion` values no longer fall back to `0.16.7`.

## Compatibility

- Extension IndexedDB remains version 13.
- Notes PWA IndexedDB remains version 1.
- No Raw Evidence, Evidence URI, Agent Bundle, Portable Capture, Project State, Memory Gate or recovery schema change.
