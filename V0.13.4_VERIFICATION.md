# KV Archive v0.13.4 Verification

Status: `LOCALLY_VERIFIED`

## Automated suite

```text
npm test
175 tests
175 passed
0 failed
```

The full suite covers existing archive, Conversation Basket, provider foundation, local vault, snapshots, scheduler, Agent Bridge, Continuity Benchmark, Project State, Memory and Obsidian behavior, plus v0.13.4 capture-hardening regressions.

## Static and build checks

```text
npm run typecheck
npm run build
node --check apps/extension/src/background.js
node --check apps/extension/src/generic-dom-adapter.js
```

All passed.

## Capture-hardening verification

Verified locally with deterministic fixtures:

- a two-message structured tree is rejected as confidently complete when the page exposes a much larger turn lower bound;
- a continuous 100-message tree remains eligible for direct verified capture;
- a small tree is corroborated only by a completed, matching hydration pass;
- incomplete hydration does not upgrade structured completeness;
- role-less overlapping virtual windows reconstruct one globally ordered user/assistant sequence;
- repeated text with distinct stable IDs remains present;
- scroll restoration preserves distance from bottom after document and viewport growth;
- export source contains one download call;
- hydration iteration diagnostics propagate to the HTML diagnostic layer.

## Performance regression

### Historical archive

- conversations: 1,200;
- approximate source text: 19,660,800 bytes;
- ZIP volumes: 29;
- maximum concurrent artifact reads: 1;
- peak heap delta: 19,959,792 bytes;
- peak RSS delta: 96,690,176 bytes.

### Snapshot

- conversations: 3,000;
- unique assets: 120;
- changed conversations: 200;
- heap peak delta: 12,701,912 bytes;
- snapshot, retention and garbage-collection smoke passed.

### Obsidian

- conversations: 3,000;
- nodes: 3,002;
- edges: 6,001;
- volumes: 2;
- status: COMPLETE;
- broken links: 0;
- invalid Canvas references: 0.

## Version consistency

- root package: 0.13.4;
- extension source manifest: 0.13.4;
- built extension manifest: 0.13.4;
- Agent Bridge package: 0.13.4.

## Not verified in this environment

- the exact original private long conversation in the user's authenticated Chrome profile;
- the current production ChatGPT DOM and internal response shape;
- scroll-container selection across every ChatGPT layout experiment;
- very slow or interrupted network behavior beyond deterministic bounded simulations;
- real regenerated-branch selection through DOM-only fallback;
- in-place Chrome extension upgrade with existing user IndexedDB;
- Git commit, push, PR, remote CI or public release.
