# KV Archive v0.14.1 Changeset

## Added

- Memory Gate review UI in `memory.html` / `memory.js` / `memory.css`.
- Local Gate config versions and immutable run receipts in IndexedDB v10.
- Receipt export and policy restoration.
- Optional governance metadata for Project State v1 decision/task records.
- Shared Memory Gate core synchronization between Agent and extension.
- Paired Gate-off / Gate-on Continuity Benchmark experiment and scoring CLI.
- Failure taxonomy and per-dimension benchmark deltas.
- Agent Bridge shell and CMD helpers for paired benchmark runs.

## Changed

- Extension database schema 9 → 10.
- Product version 0.14.0 → 0.14.1.
- Invalid expiry metadata now enters REVIEW with `INVALID_EXPIRY`.
- Memory Gate receipts reject duplicate IDs instead of overwriting.
- Roadmap shifts Unified Capture / Note / Stellar Graph Contract to v0.14.2.

## Protected

No changes to Raw Evidence semantics, stable Evidence URIs, existing approved Project State versions, approved Memory versions, backup checkpoints, Conversation Basket stores, Obsidian stable paths or non-Gated Context Packs.
