# KV Archive v0.12.2｜最后一公里体验

## 版本目标

把已经存在的备份、资料库、导出与 Obsidian 能力串成普通用户可以直接完成的路径。功能深度保留在高级层，首屏只回答：当前能做什么、数据保存在哪里、下一步是什么、出错后如何恢复。

## 本次实现

### 1. 状态感知弹窗

弹窗会根据当前页面、未完成备份和资料库状态推荐下一步：

- 未打开具体 ChatGPT 对话：打开 ChatGPT；
- 有未完成备份：继续备份；
- 浏览器已有备份但电脑分卷未完整生成：生成电脑备份；
- 备份已完成但资料库为空：加入资料库；
- 资料库已有内容：搜索资料库；
- 新用户：先保存当前对话。

常用入口只保留备份中心和本地资料库；项目状态、记忆同步和 Obsidian 收进高级区域。

### 2. 当前对话默认进入本地资料库

“保存当前对话”现在默认写入本地资料库，下载文件变为可选动作。用户可以同时选择下载 HTML、Markdown 或 ZIP；至少需要选择一个保存目标。

保存成功后直接提供：

- 立即查看本地资料库；
- 显示下载文件。

Raw Evidence 仍保存在本地资料库中，默认下载文件继续遵守 v0.12.1 的内容降噪边界。

### 3. 备份健康卡和三层状态

备份中心首屏区分：

- 浏览器本地是否已保存；
- 电脑 ZIP 分卷是否已生成；
- 浏览器存储占用和容量估算；
- 是否获得持久化存储保护。

详细的普通、归档、Project、附件、失败与重试统计默认收起。

### 4. 开始前只读预检

点击“开始全量备份”后先检查：

- 当前 ChatGPT 工作空间；
- 预计对话规模；
- 首批 Project 数量；
- 本次附件策略；
- 浏览器剩余容量；
- 是否存在可以继续的检查点。

确认后才开始采集。预检不会修改 ChatGPT 内容。

### 5. 行动型错误恢复

常见错误不再只显示报错文字，而是提供对应动作：

- 打开 ChatGPT；
- 刷新登录后重试；
- 检查工作空间；
- 查看下载记录；
- 重新检测或重试。

### 6. Obsidian 简明质量结论

Obsidian 页面在技术指标之前先给出：

- 可以安全导出；
- 可以导出，但有少量缺失；
- 暂不建议导出。

重复节点、悬空关系、证据缺失、附件缺失和 Canvas 引用错误仍保留在可展开的技术报告中。

## 工程边界

- 版本提升到 `0.12.2`；
- 不修改 IndexedDB schema；
- 不删除 Raw Evidence；
- 不改变全量备份、定时备份、分卷、快照或恢复算法；
- 不减少高级设置和技术质量指标；
- 不引入新的远程服务或模型调用。

## 当前验证级别

状态：`LOCALLY_VERIFIED`

自动化、构建和性能结果见 `V0.12.2_VERIFICATION.md`。真实 Chrome 账号、原地升级、多窗口工作空间、下载目录和真实 Obsidian 导入仍需按 `REAL_V0.12.2_ACCEPTANCE.md` 验收。
